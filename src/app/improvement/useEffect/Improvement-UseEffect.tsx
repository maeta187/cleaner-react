'use client'

import { useEffect, useState } from 'react'

/**
 * useEffectを使用せずに非同期処理を行う方法はいくつかある
 * 1. フレームワークを使用している場合は、組込みのデータフェッチ機能を使用する
 * 2. ReactQueryやSWRなどのライブラリを使用する
 */
export const ImprovementUseEffect = () => {
  /**
   * フレームワークやライブラリを使用して非同期処理を行う
   */
  // const { data, isLoading, error } = useData('api/sample', { filterStatus });
  return <div>TodoList</div>
}
